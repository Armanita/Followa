'use client';

import { useEffect, useState } from 'react';
import { api, getFileObjectUrl } from '@/lib/api';
import { btnPrimary, Field, inputClass, Modal, Spinner, useToast } from '@/components/ui';
import { faDate, faDateInput, jalaliDateToIso } from '@/lib/jalali';

export interface PersonnelTarget { membershipId: string; fullName: string; }
interface PersonnelProfileResponse { user: { id: string; firstName: string; lastName: string; nationalId: string | null; birthDate: string | null; phone: string | null; address: string | null; maritalStatus: 'SINGLE' | 'MARRIED' | null; bankCardNumber: string | null; bankIban: string | null; bankName: string | null; profileFinalizedAt: string | null; hasPersonnelPhoto: boolean; }; membership: { id: string; role: 'COMPANY_MANAGER' | 'EMPLOYEE'; isActive: boolean; jobTitle: string | null; employeeCode: string | null; }; company: { id: string; name: string }; }
type FormState = Pick<PersonnelProfileResponse['user'], 'firstName' | 'lastName' | 'nationalId' | 'phone' | 'address' | 'maritalStatus' | 'bankCardNumber' | 'bankIban' | 'bankName'>;

export function PersonnelProfileModal({ target, onClose, onUpdated }: { target: PersonnelTarget | null; onClose: () => void; onUpdated: () => void }) {
  const toast = useToast(); const [data, setData] = useState<PersonnelProfileResponse | null>(null); const [form, setForm] = useState<FormState | null>(null); const [birthDate, setBirthDate] = useState(''); const [loading, setLoading] = useState(false); const [busy, setBusy] = useState(false); const [photoBusy, setPhotoBusy] = useState(false); const [photoUrl, setPhotoUrl] = useState<string | null>(null);
  useEffect(() => () => { if (photoUrl) URL.revokeObjectURL(photoUrl); }, [photoUrl]);
  useEffect(() => { if (!target) { setData(null); setForm(null); setBirthDate(''); setPhotoUrl(null); return; } let cancelled = false; setLoading(true); api.get<PersonnelProfileResponse>(`/members/${target.membershipId}/profile`).then(async (res) => { if (cancelled) return; setData(res); setForm({ firstName: res.user.firstName, lastName: res.user.lastName, nationalId: res.user.nationalId, phone: res.user.phone, address: res.user.address, maritalStatus: res.user.maritalStatus, bankCardNumber: res.user.bankCardNumber, bankIban: res.user.bankIban, bankName: res.user.bankName }); setBirthDate(res.user.birthDate ? faDateInput(res.user.birthDate) : ''); if (res.user.hasPersonnelPhoto) { try { const photo = await getFileObjectUrl(`/members/${target.membershipId}/photo`); if (!cancelled) setPhotoUrl(photo.url); else URL.revokeObjectURL(photo.url); } catch { if (!cancelled) setPhotoUrl(null); } } else setPhotoUrl(null); }).catch((err) => toast.error(err instanceof Error ? err.message : 'خطا در دریافت اطلاعات پرسنلی')).finally(() => { if (!cancelled) setLoading(false); }); return () => { cancelled = true; }; }, [target, toast]);
  const set = (key: keyof FormState) => (e: React.ChangeEvent<HTMLInputElement | HTMLSelectElement | HTMLTextAreaElement>) => setForm((current) => current ? { ...current, [key]: e.target.value || null } : current);
  const refreshPhoto = async () => { if (!target) return; const photo = await getFileObjectUrl(`/members/${target.membershipId}/photo`); setPhotoUrl(photo.url); };
  const uploadPhoto = async (file: File) => { if (!target) return; setPhotoBusy(true); try { const body = new FormData(); body.append('file', file); await api.post(`/members/${target.membershipId}/photo`, body); await refreshPhoto(); setData((current) => current ? { ...current, user: { ...current.user, hasPersonnelPhoto: true } } : current); toast.success('عکس پرسنلی به‌روزرسانی شد'); onUpdated(); } catch (err) { toast.error(err instanceof Error ? err.message : 'خطا در بارگذاری عکس'); } finally { setPhotoBusy(false); } };
  const save = async (e: React.FormEvent) => { e.preventDefault(); if (!target || !form) return; setBusy(true); try { const res = await api.patch<{ message: string }>(`/members/${target.membershipId}/profile`, { ...form, birthDate: birthDate.trim() ? jalaliDateToIso(birthDate) : null }); toast.success(res.message); onUpdated(); onClose(); } catch (err) { toast.error(err instanceof Error ? err.message : 'خطا در ذخیره اطلاعات پرسنلی'); } finally { setBusy(false); } };
  return <Modal open={Boolean(target)} onClose={onClose} title={target ? `اطلاعات پرسنلی — ${target.fullName}` : 'اطلاعات پرسنلی'} wide>{loading || !data || !form ? <Spinner /> : <form onSubmit={save} className="space-y-4"><div className="flex items-center gap-4 rounded-2xl border border-workspace-border bg-workspace-elevated/60 p-4"><div className="grid h-24 w-24 shrink-0 place-items-center overflow-hidden rounded-2xl border border-brand-400/20 bg-brand-400/10 text-xl font-black text-brand-200">{photoUrl ? <img src={photoUrl} alt="عکس پرسنلی" className="h-full w-full object-cover" /> : data.user.firstName.charAt(0)}</div><div className="min-w-0 flex-1"><p className="text-sm font-black text-white">{data.user.firstName} {data.user.lastName}</p><p className="mt-1 text-[10px] text-workspace-soft">{data.membership.jobTitle || 'بدون سمت'}{data.membership.employeeCode ? ` · کد ${data.membership.employeeCode}` : ''}</p><label className="mt-3 inline-flex cursor-pointer rounded-lg border border-workspace-borderStrong bg-workspace-surface px-3 py-2 text-[10px] font-semibold text-workspace-muted hover:bg-workspace-hover hover:text-white">{photoBusy ? 'در حال بارگذاری…' : data.user.hasPersonnelPhoto ? 'تعویض عکس' : 'افزودن عکس'}<input type="file" className="hidden" accept="image/jpeg,image/png,image/webp" disabled={photoBusy} onChange={(e) => { const file = e.target.files?.[0]; if (file) void uploadPhoto(file); e.currentTarget.value = ''; }} /></label><p className="mt-1 text-[9px] text-workspace-soft">JPG، PNG یا WebP تا ۵ مگابایت</p></div></div>{data.user.profileFinalizedAt && <div className="rounded-xl border border-emerald-400/20 bg-emerald-400/10 px-3 py-2.5 text-[10px] leading-5 text-emerald-200">اطلاعات توسط کارمند در {faDate(data.user.profileFinalizedAt)} ثبت نهایی شده است؛ ویرایش مدیریتی همچنان مجاز است.</div>}<div className="grid grid-cols-1 gap-3 sm:grid-cols-2"><Field label="نام" required><input className={inputClass} value={form.firstName} onChange={set('firstName')} /></Field><Field label="نام خانوادگی" required><input className={inputClass} value={form.lastName} onChange={set('lastName')} /></Field><Field label="کد ملی"><input className={`${inputClass} tnum`} dir="ltr" inputMode="numeric" maxLength={10} value={form.nationalId ?? ''} onChange={set('nationalId')} /></Field><Field label="تاریخ تولد شمسی"><input className={`${inputClass} tnum`} dir="ltr" placeholder="۱۴۰۵/۰۶/۰۷" value={birthDate} onChange={(e) => setBirthDate(e.target.value)} /></Field><Field label="تلفن"><input className={`${inputClass} tnum`} dir="ltr" value={form.phone ?? ''} onChange={set('phone')} /></Field><Field label="وضعیت تأهل"><select className={inputClass} value={form.maritalStatus ?? ''} onChange={set('maritalStatus')}><option value="">— انتخاب —</option><option value="SINGLE">مجرد</option><option value="MARRIED">متأهل</option></select></Field></div><Field label="آدرس"><textarea className={`${inputClass} min-h-20`} value={form.address ?? ''} onChange={set('address')} /></Field><div className="border-t border-workspace-border pt-4"><p className="mb-3 text-xs font-black text-white">اطلاعات بانکی</p><div className="grid grid-cols-1 gap-3 sm:grid-cols-2"><Field label="شماره کارت"><input className={`${inputClass} tnum`} dir="ltr" inputMode="numeric" value={form.bankCardNumber ?? ''} onChange={set('bankCardNumber')} /></Field><Field label="شماره شبا"><input className={`${inputClass} tnum`} dir="ltr" placeholder="IR..." value={form.bankIban ?? ''} onChange={set('bankIban')} /></Field><Field label="نام بانک"><input className={inputClass} value={form.bankName ?? ''} onChange={set('bankName')} /></Field></div></div><button disabled={busy} className={btnPrimary}>{busy ? 'در حال ذخیره…' : 'ذخیره تغییرات پرسنلی'}</button></form>}</Modal>;
}
